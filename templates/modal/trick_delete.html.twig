<div class="col-md-4">
    <div class="modal fade" id="modalTrickDelete{{ trick.id }}" tabindex="-1" role="dialog" aria-labelledby="modalTrickDelete" aria-hidden="true">
        <div class="modal-dialog modal-danger modal-dialog-centered modal-" role="document">
            <div class="modal-content bg-gradient-danger">
                <div class="modal-header">
                    <h6 class="modal-title" id="modal-title-notification">Confirmation de suppression</h6>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">×</span>
                    </button>
                </div>
                <div class="modal-body">
                    <div class="py-3 text-center">
                        <i class="ni ni-bell-55 ni-3x"></i>
                        <p>
                            Vous êtes sur le point de supprimer la figure {{ trick.name }}.
                            <br>
                            Souhaitez-vous continuer ?
                        </p>
                    </div>
                </div>
                <div class="modal-footer">
                    <a class="btn btn-link text-white" href={{ path('trick_delete', { 'slug': trick.slug }) }}>Oui, supprimer la figure !</a>
                    <button type="button" class="btn btn-link text-white ml-auto" data-dismiss="modal">Fermer</button>
                </div>
            </div>
        </div>
    </div>
</div>